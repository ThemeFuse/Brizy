import {
  FromElementModel,
  ToElementModel
} from "visual/component/Options/Type";

export const defaultValue = "";

export const fromElementModel: FromElementModel<"popupCondition"> = () => ({});

export const toElementModel: ToElementModel<"popupCondition"> = () => ({});
