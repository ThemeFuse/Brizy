import { defaultValueValue } from "visual/utils/onChange";

export function styleElementMegaMenu({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("megaMenu");
}

export function styleElementMegaMenuWidth({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("megaMenuWidth");
}

export function styleElementMegaMenuWidthSuffix({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("megaMenuWidthSuffix");
}

export function styleElementMegaMenuOffsetTop({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("megaMenuOffsetTop");
}

export function styleElementMegaMenuHeight({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("sectionHeight");
}

export function styleElementMegaMenuHeightStyle({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("sectionHeightStyle");
}

export function styleElementMegaMenuHeightSuffix({ v, device, state }) {
  const dvv = key => defaultValueValue({ v, key, device, state });

  return dvv("sectionHeightSuffix");
}
