import React, { ReactElement } from "react";
import { FCC } from "visual/utils/react/types";

export const FatIconsGrid: FCC = ({ children }): ReactElement => {
  return <div className="brz-ed--fat-icons-grid">{children}</div>;
};
