import { GlobalPopup } from "../types/GlobalBlocks";
// import { t } from "../utils/i18n";

export const globalPopups: GlobalPopup = {
  // async get(res, rej) {}
  // async getByRules(res, rej, { uid }) {},
  // async create(res, rej, extra) {},
  // async update(res, rej, extra) {},
  // async delete(res, rej, extra) {}
};
